# Процесс авторизации - Новая логика

## Как это работает

### 1. Валидация при вводе (validate-admin.js)

Когда вы вводите логин и пароль администратора в `setup.sh`:

```bash
? Email администратора: admin@localhost  
? Пароль администратора: ********

ℹ Проверка учетных данных...
```

**Что происходит:**
1. ✅ Запускается `validate-admin.js`
2. ✅ Проверяются учетные данные через `pb.collection('_superusers').authWithPassword()`
3. ✅ **При успехе - сохраняется токен авторизации** в файл `.pb-auth-token`
4. ✅ Токен используется для всех последующих операций

**Результат:**
```bash
✓ Авторизация успешна!
ℹ ID администратора: abc123xyz
```

**Сохраняется файл `.pb-auth-token`:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "adminId": "abc123xyz",
  "adminEmail": "admin@localhost",
  "url": "http://127.0.0.1:54739",
  "timestamp": "2024-12-01T12:00:00.000Z"
}
```

---

### 2. Создание коллекций (create-collections.js)

При создании коллекций **НЕ происходит** повторная авторизация!

```bash
▶ Создание коллекций PocketBase
? Создать коллекции в PocketBase сейчас? [Y/n]: y

🔄 Подключение к PocketBase...
✓ PocketBase доступен

🔐 Использование сохраненной авторизации...
✓ Авторизован как: admin@localhost
  (токен создан: 01.12.2024, 12:00:00)
```

**Что происходит:**
1. ✅ Скрипт **читает** файл `.pb-auth-token`
2. ✅ **Загружает токен** из файла
3. ✅ Устанавливает токен в `pb.authStore`
4. ✅ Работает с PocketBase **без повторной авторизации**

**Код:**
```javascript
// Загружаем сохраненный токен
const authData = JSON.parse(fs.readFileSync('.pb-auth-token', 'utf8'));

// Устанавливаем токен в authStore
pb.authStore.save(authData.token, null);

// Теперь можем работать с API
await pb.collections.create(...);
```

---

## Преимущества новой логики

### ✅ Авторизация один раз
- Вводите логин/пароль **только 1 раз**
- Токен сохраняется и переиспользуется
- Не нужно вводить пароль для каждого скрипта

### ✅ Быстрая валидация
- Узнаёте об ошибке **сразу** при вводе
- Не тратите время на установку если данные неверные

### ✅ Безопасность
- Токен хранится в файле с правами `600` (только владелец)
- Токен добавлен в `.gitignore`
- Не передается пароль между скриптами

### ✅ Удобство отладки
- Если токен протух - видите понятное сообщение
- Можете вручную удалить `.pb-auth-token` и пересоздать
- Видите когда и кем был создан токен

---

## Структура файлов

```
backend-setup/
├── validate-admin.js       # Создает .pb-auth-token
├── create-collections.js   # Использует .pb-auth-token
├── check-admin.js          # Проверка (не использует токен)
├── .pb-auth-token          # ТОКЕН (не коммитится!)
├── .gitignore              # Игнорирует .pb-auth-token
└── .env                    # Конфигурация
```

---

## Примеры использования

### Полная установка через setup.sh

```bash
./setup.sh

# 1. Валидация
? Email администратора: admin@localhost
? Пароль администратора: ********
ℹ Проверка учетных данных...
✓ Авторизация успешна!
ℹ ID администратора: abc123xyz

# Создан файл .pb-auth-token ✓

# 2. Создание коллекций (использует токен)
▶ Создание коллекций PocketBase
🔐 Использование сохраненной авторизации...
✓ Авторизован как: admin@localhost

# ПАРОЛЬ НЕ ЗАПРАШИВАЕТСЯ ПОВТОРНО! ✓
```

---

### Ручной запуск validate-admin.js

```bash
# Создать/обновить токен
node validate-admin.js admin@localhost mypassword http://127.0.0.1:54739

# Результат:
SUCCESS
{
  "id": "abc123xyz",
  "email": "admin@localhost",
  "created": "2024-12-01...",
  "tokenSaved": true
}

# Файл .pb-auth-token создан ✓
```

---

### Ручной запуск create-collections.js

```bash
# ВАЖНО: Сначала создайте токен!
node validate-admin.js admin@localhost mypassword

# Теперь создайте коллекции
node create-collections.js

# Результат:
🔐 Использование сохраненной авторизации...
✓ Авторизован как: admin@localhost
📋 Получение списка коллекций...
📦 Создание коллекции "users"...
✓ Коллекция "users" создана
...
```

---

## Что делать если...

### Токен протух

```bash
$ node create-collections.js

✗ Ошибка загрузки токена: Unauthorized

💡 Попробуйте:
   1. Удалить файл .pb-auth-token
   2. Запустить setup.sh снова
   3. Или выполнить: node validate-admin.js admin@localhost your_password
```

**Решение:**
```bash
# Пересоздайте токен
rm .pb-auth-token
node validate-admin.js admin@localhost mypassword

# Или запустите setup.sh снова
./setup.sh
```

---

### Токен не найден

```bash
$ node create-collections.js

✗ Токен авторизации не найден!

❌ Сначала выполните авторизацию:
   1. Запустите setup.sh 
   2. Или выполните: node validate-admin.js admin@localhost your_password
```

**Решение:**
```bash
# Создайте токен
node validate-admin.js admin@localhost mypassword

# Теперь запустите скрипт
node create-collections.js
```

---

### Неверный пароль при валидации

```bash
$ node validate-admin.js admin@localhost wrongpassword

FAILED
{
  "status": 400,
  "message": "Failed to authenticate.",
  "data": {}
}

# Токен НЕ создан ✗
```

**Решение:**
```bash
# Используйте правильный пароль
node validate-admin.js admin@localhost correctpassword

SUCCESS
{
  "id": "abc123xyz",
  "email": "admin@localhost",
  "created": "2024-12-01...",
  "tokenSaved": true
}

# Токен создан ✓
```

---

## Безопасность

### ✅ Что безопасно:

1. **Токен имеет права 600**
   ```bash
   $ ls -la .pb-auth-token
   -rw------- 1 user user 456 Dec  1 12:00 .pb-auth-token
   ```

2. **Токен в .gitignore**
   ```bash
   $ cat .gitignore
   .pb-auth-token
   ```

3. **Пароль не сохраняется**
   - Только токен JWT
   - Токен имеет срок действия (expiry)

### ⚠️ Важные правила:

1. **НЕ коммитьте `.pb-auth-token`** в git
2. **НЕ передавайте токен** другим пользователям
3. **Удаляйте токен** после завершения работы:
   ```bash
   rm .pb-auth-token
   ```

4. **Используйте разные токены** для разных окружений:
   ```bash
   # Dev
   node validate-admin.js admin@localhost devpass http://localhost:54739
   
   # Prod  
   node validate-admin.js admin@prod.com prodpass http://prod.com:54739
   ```

---

## Диаграмма процесса

```
┌─────────────────────────────────────────┐
│  ШАГИ АВТОРИЗАЦИИ                       │
└─────────────────────────────────────────┘

1. Ввод данных в setup.sh
   ↓
   Email: admin@localhost
   Password: ********
   ↓
   
2. validate-admin.js
   ↓
   pb.collection('_superusers').authWithPassword()
   ↓
   Успех? → YES
   ↓
   Сохранить токен в .pb-auth-token
   ↓
   {
     token: "eyJhbGci...",
     adminId: "abc123",
     adminEmail: "admin@localhost",
     url: "http://127.0.0.1:54739",
     timestamp: "2024-12-01..."
   }
   ↓
   ✓ Авторизация успешна!
   
   
3. create-collections.js
   ↓
   Читать .pb-auth-token
   ↓
   pb.authStore.save(authData.token, null)
   ↓
   ✓ Авторизован как: admin@localhost
   ↓
   await pb.collections.create(...)
   ↓
   ✓ Коллекции созданы!
```

---

## Итог

### Старая логика ❌
```bash
setup.sh
  ├─ validate-admin.js → Авторизация #1
  └─ create-collections.js → Авторизация #2 (повторная!)
```

**Проблемы:**
- 2 авторизации
- Медленнее
- Ошибка видна только в конце

### Новая логика ✅
```bash
setup.sh
  ├─ validate-admin.js → Авторизация + Сохранить токен
  └─ create-collections.js → Использовать токен
```

**Преимущества:**
- 1 авторизация
- Быстрее
- Ошибка видна сразу
- Токен переиспользуется
- Безопаснее

---

**Документация:**
- [README.md](README.md) - Обзор
- [USAGE.md](USAGE.md) - Руководство
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем
- [VALIDATION-EXAMPLE.md](VALIDATION-EXAMPLE.md) - Примеры валидации

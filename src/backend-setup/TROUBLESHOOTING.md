# Troubleshooting - Решение проблем

## Ошибка: "The requested resource wasn't found"

### Симптомы
```bash
🔐 Авторизация администратора...
Используются учетные данные из .env файла
✗ Ошибка авторизации: The requested resource wasn't found.
```

### Причина
**В новых версиях PocketBase (v0.20+)** endpoint для авторизации администратора изменился:
- Старый: `/api/admins/auth-with-password`
- Новый: `/api/collections/_superusers/auth-with-password`

Скрипты обновлены и используют новый endpoint. Если вы видите эту ошибку, возможны две причины:
1. Администратор не создан
2. Неверные учетные данные

### Решение

#### Шаг 1: Проверка наличия администратора

```bash
cd backend-setup
node check-admin.js
```

Этот скрипт покажет:
- ✅ Если администратор создан
- ❌ Если администратора нет

#### Шаг 2: Создание администратора

1. Откройте в браузере: **http://127.0.0.1:54739/_/**

2. Вы увидите одну из двух форм:

**A) Форма создания администратора** (если админа нет):
```
┌─────────────────────────────────────┐
│ Create your admin account           │
│                                     │
│ Email:    [                    ]    │
│ Password: [                    ]    │
│                                     │
│         [Create admin]              │
└─────────────────────────────────────┘
```

- Введите email (например: `admin@localhost`)
- Введите пароль (минимум 8 символов)
- Нажмите "Create admin"

**B) Форма входа** (если админ уже создан):
```
┌─────────────────────────────────────┐
│ Admin login                         │
│                                     │
│ Email:    [                    ]    │
│ Password: [                    ]    │
│                                     │
│         [Login]                     │
└─────────────────────────────────────┘
```

- Это значит что администратор уже создан
- Используйте его учетные данные в setup.sh

#### Шаг 3: Обновите .env файл

```bash
cd backend-setup
nano .env
```

Добавьте или обновите:
```env
POCKETBASE_ADMIN_EMAIL=admin@localhost
POCKETBASE_ADMIN_PASSWORD=ваш_пароль
```

#### Шаг 4: Повторите установку

```bash
./setup.sh
```

Или запустите только создание коллекций:
```bash
node create-collections.js
```

---

## Ошибка: "Invalid email or password"

### Симптомы
```bash
✗ Ошибка авторизации: Invalid email or password
```

### Причина
Неверные учетные данные в .env файле.

### Решение

1. Проверьте email в .env:
   ```bash
   cat backend-setup/.env | grep POCKETBASE_ADMIN_EMAIL
   ```

2. Попробуйте войти вручную:
   - Откройте http://127.0.0.1:54739/_/
   - Используйте те же учетные данные
   - Если не получилось - сбросьте пароль

3. Обновите .env с правильными данными

---

## Ошибка: PocketBase недоступен

### Симптомы
```bash
✗ PocketBase недоступен: fetch failed
```

### Причина
PocketBase не запущен или работает на другом порту.

### Решение

#### 1. Проверьте запущен ли PocketBase:

```bash
curl http://127.0.0.1:54739/api/health
```

**Ожидаемый результат:**
```json
{"message":"API is healthy.","code":200,"data":{}}
```

#### 2. Если не отвечает - запустите PocketBase:

```bash
# Найдите процесс
ps aux | grep pocketbase

# Если не запущен
cd /opt/pocketbase
./pocketbase serve --http=127.0.0.1:54739
```

#### 3. Проверьте порт в .env:

```bash
cat backend-setup/.env | grep VITE_POCKETBASE_URL
```

Должно быть:
```env
VITE_POCKETBASE_URL=http://127.0.0.1:54739
```

#### 4. Если используете nginx:

Убедитесь что nginx проксирует на правильный порт:

```nginx
location ^~ /api/ {
    proxy_pass http://127.0.0.1:54739;
    # ...
}
```

---

## Ошибка: Redis не доступен

### Симптомы
```bash
✗ Redis не отвечает на localhost:6379
```

### Решение

#### 1. Проверьте статус:

```bash
sudo systemctl status redis-server
```

#### 2. Запустите Redis:

```bash
sudo systemctl start redis-server
```

#### 3. Проверьте подключение:

```bash
redis-cli -h localhost -p 6379 ping
```

**Ожидаемый результат:** `PONG`

#### 4. Если не помогло:

```bash
# Проверьте логи
sudo journalctl -u redis-server -n 50

# Переустановите
sudo apt remove redis-server
sudo apt install redis-server
sudo systemctl start redis-server
```

---

## Ошибка: NPM пакеты не устанавливаются

### Симптомы
```bash
npm ERR! code ENOENT
```

### Решение

#### 1. Очистите кэш:

```bash
npm cache clean --force
```

#### 2. Удалите node_modules:

```bash
cd backend-setup
rm -rf node_modules package-lock.json
npm install
```

#### 3. Проверьте версию Node.js:

```bash
node -v
```

Должно быть: `v18.0.0` или выше

#### 4. Обновите NPM:

```bash
npm install -g npm@latest
```

---

## Ошибка: Коллекции не создаются

### Симптомы
```bash
✗ Ошибка создания коллекции "users"
```

### Причина
- Нет прав администратора
- Коллекция уже существует
- Ошибка в схеме данных

### Решение

#### 1. Проверьте авторизацию:

```bash
cd backend-setup
node check-admin.js
```

#### 2. Проверьте существующие коллекции:

- Откройте http://127.0.0.1:54739/_/
- Перейдите в "Collections"
- Удалите дублирующиеся коллекции если нужно

#### 3. Запустите скрипт с отладкой:

```bash
DEBUG=* node create-collections.js
```

---

## Полезные команды для диагностики

### Проверка всех сервисов:

```bash
# PocketBase
curl http://127.0.0.1:54739/api/health

# Redis
redis-cli ping

# Node.js
node -v

# NPM пакеты
cd backend-setup
npm list pocketbase ioredis dotenv
```

### Проверка портов:

```bash
# Что слушает порт 54739
sudo lsof -i :54739

# Что слушает порт 6379
sudo lsof -i :6379
```

### Логи:

```bash
# PocketBase logs (если через systemd)
sudo journalctl -u pocketbase -f

# Redis logs
sudo journalctl -u redis-server -f

# Или в файлах
tail -f /opt/pocketbase/pocketbase.log
```

---

## Быстрая проверка (Checklist)

Используйте этот чеклист для быстрой диагностики:

```bash
# 1. PocketBase доступен?
curl http://127.0.0.1:54739/api/health
# ✓ Должно вернуть: {"message":"API is healthy."...}

# 2. Redis работает?
redis-cli ping
# ✓ Должно вернуть: PONG

# 3. Администратор создан?
cd backend-setup && node check-admin.js
# ✓ Должно сказать: "Администратор СОЗДАН"

# 4. .env файл существует?
ls -la backend-setup/.env .env
# ✓ Должно показать оба файла

# 5. NPM пакеты установлены?
ls backend-setup/node_modules/pocketbase
# ✓ Должна существовать директория

# 6. Коллекции созданы?
# Откройте http://127.0.0.1:54739/_/
# ✓ Должно быть 7 коллекций
```

---

## Все еще не работает?

### Полная переустановка:

```bash
# 1. Остановите все сервисы
sudo systemctl stop pocketbase
sudo systemctl stop redis-server

# 2. Очистите данные
rm -rf backend-setup/.env .env
rm -rf backend-setup/node_modules
rm -rf node_modules

# 3. Удалите данные PocketBase (ОСТОРОЖНО!)
# rm -rf /opt/pocketbase/pb_data

# 4. Запустите сервисы
sudo systemctl start redis-server
sudo systemctl start pocketbase

# 5. Создайте администратора
# Откройте http://127.0.0.1:54739/_/

# 6. Запустите setup снова
cd backend-setup
./setup.sh
```

### Получить помощь:

1. Проверьте логи:
   ```bash
   sudo journalctl -xe
   ```

2. Запустите полную диагностику:
   ```bash
   cd backend-setup
   node test-connection.js
   ```

3. Создайте issue в репозитории с:
   - Версией Node.js (`node -v`)
   - Версией PocketBase
   - Логами ошибок
   - Результатом `node check-admin.js`

---

## Контакты

- GitHub Issues: [ссылка на репозиторий]
- Документация: [DOCS.md](../DOCS.md)
# 🔧 Устранение неполадок - Конверт

Руководство по решению распространенных проблем при использовании приложения.

---

## 🚨 Проблемы с аутентификацией

### ❌ "Invalid login credentials"

**Полное сообщение об ошибке:**
```
Supabase signin error: AuthApiError: Invalid login credentials
status: 400
code: "invalid_credentials"
```

**Причина:**
Вы пытаетесь войти с учетными данными пользователя, который **не существует** в базе данных.

**Решения:**

#### ✅ Решение 1: Быстрый вход (самый простой)
1. На экране входа нажмите кнопку **"🚀 Быстрый вход (тестовый пользователь)"**
2. Приложение автоматически создаст тестового пользователя и выполнит вход
3. Готово! Вы в системе

#### ✅ Решение 2: Через DevPanel
1. На экране входа нажмите иконку **⚙️** (шестеренка) в правом верхнем углу
2. Нажмите **"✨ Создать тестового пользователя"**
3. Закройте DevPanel (нажмите снова на ⚙️)
4. Войдите с данными:
   - **Email:** `test@example.com`
   - **Пароль:** `test12345678`

#### ✅ Решение 3: Регистрация
1. Нажмите **"Зарегистрироваться"** на экране входа
2. Заполните форму регистрации
3. Вы автоматически войдете после успешной регистрации

---

### ❌ "Invalid token" / "missing sub claim"

**Полное сообщение об ошибке:**
```
getUserFromToken: Supabase validation error: invalid claim: missing sub claim
Invalid token - user not found
```

**Причина:**
В localStorage хранится **старый или поврежденный** JWT токен.

**Решения:**

#### ✅ Автоматическое решение
Приложение **автоматически** обнаруживает и удаляет невалидные токены. Просто:
1. Перезагрузите страницу
2. Войдите заново

#### ✅ Ручная очистка (если автоматическое не сработало)
1. Откройте консоль браузера (F12)
2. Перейдите на вкладку **Console**
3. Выполните команду:
   ```javascript
   localStorage.clear();
   ```
4. Перезагрузите страницу
5. Войдите заново

---

### ❌ "Пользователь не найден"

**Причина:**
Пользователь существует в Supabase Auth, но **отсутствует в KV хранилище**.

**Решения:**

#### ✅ Решение 1: Попробуйте войти снова
Приложение автоматически синхронизирует данные при входе.

#### ✅ Решение 2: Проверьте через DevPanel
1. Откройте DevPanel (⚙️)
2. Посмотрите список пользователей
3. Проверьте индикаторы:
   - 🟢 Зелёная галочка = в Supabase Auth
   - 🔵 Синяя галочка = в KV хранилище
4. Если есть только зелёная галочка - попробуйте удалить пользователя и создать заново

#### ✅ Решение 3: Пересоздайте пользователя
1. Откройте DevPanel (⚙️)
2. Удалите проблемного пользователя
3. Создайте нового через "✨ Создать тестового пользователя"

---

## 🔐 Проблемы со входом

### ❌ "Email не подтвержден"

**Решение:**
Эта ошибка **не должна возникать** в текущей конфигурации, так как:
- Email подтверждается автоматически при создании (`email_confirm: true`)
- Email сервер не настроен

Если видите эту ошибку:
1. Удалите пользователя через DevPanel
2. Создайте заново

---

### ❌ "Доступ заблокирован" / "Забанен"

**Причина:**
Ваш аккаунт был заблокирован модератором или администратором.

**Решение:**
1. Создайте новый аккаунт
2. ИЛИ обратитесь к администратору для разблокировки

**Для администраторов:**
Разбанить пользователя можно только через код сервера (функция пока не реализована в UI).

---

## 🛠️ Проблемы с DevPanel

### ❌ DevPanel не открывается

**Решение:**
1. Убедитесь, что вы на экране **входа** (не внутри приложения)
2. Нажмите на иконку ⚙️ в **правом верхнем углу**
3. Если не помогает - перезагрузите страницу

---

### ❌ "Ошибка загрузки пользователей" в DevPanel

**Решение:**
1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что сервер работает
3. Проверьте, что переменные окружения настроены:
   - `SUPABASE_URL`
   - `SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`

---

### ❌ Не удается создать тестового пользователя

**Ошибка:** "Тестовый пользователь уже существует"

**Решение:**
1. Пользователь уже создан - просто войдите с данными:
   - Email: `test@example.com`
   - Пароль: `test12345678`
2. ИЛИ используйте кнопку "🚀 Быстрый вход" на форме входа

---

## 💬 Проблемы с чатом

### ❌ Сообщения не отправляются

**Возможные причины и решения:**

1. **Вы замучены (muted)**
   - Проверьте, есть ли уведомление о муте
   - Обратитесь к модератору

2. **Сервер не отвечает**
   - Проверьте консоль браузера (F12)
   - Перезагрузите страницу
   - Попробуйте выйти и войти заново

3. **Нет подключения к комнате**
   - Выйдите из комнаты
   - Войдите снова

---

### ❌ Не вижу новые сообщения

**Причина:**
Real-time обновления работают через polling (каждые 3 секунды).

**Решение:**
1. Подождите 3-5 секунд
2. Если не помогает - обновите страницу
3. Проверьте консоль на ошибки

---

## 🔄 Общие проблемы

### ❌ Приложение зависло на "Загрузка..."

**Решение:**
1. Откройте консоль браузера (F12 → Console)
2. Найдите ошибки (красный текст)
3. Очистите localStorage:
   ```javascript
   localStorage.clear();
   ```
4. Перезагрузите страницу

---

### ❌ Белый экран / Ошибка сборки

**Решение:**
1. Откройте консоль браузера
2. Посмотрите на ошибку
3. Обычно это проблема с импортами или компонентами
4. Перезагрузите страницу

---

### ❌ "Failed to fetch" / Ошибки сети

**Причины:**
1. Сервер Supabase не доступен
2. Неверные переменные окружения
3. Проблемы с CORS

**Решение:**
1. Проверьте URL сервера в консоли
2. Убедитесь, что переменные окружения установлены
3. Проверьте статус Supabase: https://status.supabase.com

---

## 🧹 Полная очистка и перезапуск

Если ничего не помогает:

### Шаг 1: Очистите все данные
```javascript
// В консоли браузера (F12 → Console)
localStorage.clear();
sessionStorage.clear();
```

### Шаг 2: Очистите кэш браузера
- Chrome/Edge: Ctrl+Shift+Delete → Очистить данные
- Firefox: Ctrl+Shift+Delete → Очистить историю

### Шаг 3: Перезагрузите страницу
- Жёсткая перезагрузка: Ctrl+Shift+R

### Шаг 4: Создайте нового пользователя
- Используйте "🚀 Быстрый вход"

---

## 🔍 Отладка

### Полезные команды консоли

#### Проверить текущего пользователя
```javascript
console.log(localStorage.getItem('user'));
```

#### Проверить токен
```javascript
const token = localStorage.getItem('access_token');
console.log('Token:', token ? token.substring(0, 50) + '...' : 'null');
```

#### Проверить все данные localStorage
```javascript
for (let i = 0; i < localStorage.length; i++) {
  const key = localStorage.key(i);
  console.log(key, ':', localStorage.getItem(key));
}
```

#### Очистить конкретный ключ
```javascript
localStorage.removeItem('access_token');
```

---

## 📊 Проверка статуса системы

### Проверка DevPanel
1. Откройте DevPanel (⚙️)
2. Посмотрите список пользователей
3. Индикаторы должны быть:
   - 🟢 Auth - пользователь в Supabase Auth
   - 🔵 KV - пользователь в KV хранилище
4. Оба должны быть активны

### Проверка роутов сервера
Откройте в новой вкладке:
```
https://[ваш-project-id].supabase.co/functions/v1/make-server-b0f1e6d5/health
```

Должен вернуться:
```json
{"status": "ok"}
```

---

## 📞 Дальнейшая помощь

Если проблема не решена:

1. **Проверьте документацию:**
   - `/README.md` - основная документация
   - `/GETTING-STARTED.md` - быстрый старт
   - `/guidelines/FIRST-TIME-SETUP.md` - детальная инструкция

2. **Проверьте консоль браузера:**
   - Нажмите F12
   - Вкладка Console
   - Скопируйте текст ошибки

3. **Используйте DevPanel:**
   - Проверьте список пользователей
   - Проверьте индикаторы синхронизации
   - Попробуйте удалить и создать пользователя заново

---

## ✅ Чек-лист решения проблем

Прежде чем паниковать, проверьте:

- [ ] Я на правильной странице (экран входа для DevPanel)
- [ ] Я создал хотя бы одного пользователя
- [ ] Я использую правильные учетные данные
- [ ] Я проверил консоль браузера на ошибки
- [ ] Я попробовал очистить localStorage
- [ ] Я попробовал перезагрузить страницу
- [ ] Я попробовал использовать "🚀 Быстрый вход"
- [ ] Я проверил DevPanel
- [ ] Я прочитал сообщение об ошибке полностью

---

**Обновлено:** 29 октября 2025  
**Версия:** 1.3.0

═══════════════════════════════════════════════════════════════
  ШПАРГАЛКА КОМАНД - Система обновления приложения Конверт
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ 🚀 ПЕРВЫЙ ЗАПУСК (ОБЯЗАТЕЛЬНО!)                            │
└─────────────────────────────────────────────────────────────┘

cd /var/www/Convert/src
chmod +x update.sh init-git.sh webhook-update.sh webhook-server.js

┌─────────────────────────────────────────────────────────────┐
│ 📤 ЗАГРУЗКА В GITHUB                                        │
└─────────────────────────────────────────────────────────────┘

git add .
git commit -m "Add automatic update system"
git push origin main

┌─────────────────────────────────────────────────────────────┐
│ 🔄 ЕЖЕДНЕВНОЕ ИСПОЛЬЗОВАНИЕ                                 │
└─────────────────────────────────────────────────────────────┘

# Проверить изменения (без обновления)
./update.sh --dry-run

# Обновить приложение
./update.sh

# Обновить без вопросов
./update.sh --force

# Обновить без сборки (для проверки)
./update.sh --no-build

┌─────────────────────────────────────────────────────────────┐
│ 📊 ПРОВЕРКА СТАТУСА                                         │
└─────────────────────────────────────────────────────────────┘

# Информация о последнем обновлении
cat update-info.txt

# История коммитов
git log --oneline -10

# Текущая версия
git rev-parse --short HEAD

# Что изменилось в GitHub
git fetch origin main
git diff HEAD origin/main --stat

┌─────────────────────────────────────────────────────────────┐
│ 📝 ПРОСМОТР ЛОГОВ                                           │
└─────────────────────────────────────────────────────────────┘

# Логи обновлений (если используете cron)
tail -f /var/log/convert-update.log

# Логи webhook (если настроен)
tail -f /var/log/convert-webhook.log
tail -f /var/log/convert-webhook-update.log

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

┌─────────────────────────────────────────────────────────────┐
│ ⏰ АВТООБНОВЛЕНИЕ ЧЕРЕЗ CRON                                │
└─────────────────────────────────────────────────────────────┘

# Редактировать crontab
crontab -e

# Добавить (обновление каждый день в 3:00 AM):
0 3 * * * cd /var/www/Convert/src && ./update.sh --force >> /var/log/convert-update.log 2>&1

# Проверить активные задачи
crontab -l

┌─────────────────────────────────────────────────────────────┐
│ 🔗 WEBHOOK СЕРВЕР                                           │
└─────────────────────────────────────────────────────────────┘

# Установить зависимости
npm install express

# Сгенерировать секрет
openssl rand -hex 32

# Настроить сервис
sudo cp webhook-server.service /etc/systemd/system/
sudo nano /etc/systemd/system/convert-webhook.service
# (вставьте ваш секрет в WEBHOOK_SECRET)

# Запустить сервис
sudo systemctl daemon-reload
sudo systemctl enable convert-webhook
sudo systemctl start convert-webhook

# Проверить статус
sudo systemctl status convert-webhook

# Проверить health endpoint
curl http://localhost:9000/health

# Перезапустить
sudo systemctl restart convert-webhook

# Посмотреть логи
sudo journalctl -u convert-webhook -f

┌─────────────────────────────────────────────────────────────┐
│ 🛠️ УПРАВЛЕНИЕ ПРИЛОЖЕНИЕМ                                  │
└─────────────────────────────────────────────────────────────┘

# Пересобрать приложение
npm install
npm run build

# Dev режим
npm run dev

# Превью продакшен сборки
npm run preview

# Перезагрузить Nginx
sudo systemctl reload nginx

# Проверить статус всех сервисов
sudo systemctl status nginx pocketbase redis

┌─────────────────────────────────────────────────────────────┐
│ 🆘 ВОССТАНОВЛЕНИЕ И ОТКАТ                                   │
└─────────────────────────────────────────────────────────────┘

# Восстановить .env из бэкапа
ls -la backups/
cp backups/update-YYYYMMDD-HHMMSS/.env .env

# Откатить к предыдущей версии
git log --oneline -10
git reset --hard <commit-hash>
npm install
npm run build

# Переустановить зависимости (при проблемах)
rm -rf node_modules package-lock.json
npm install
npm run build

┌─────────────────────────────────────────────────────────────┐
│ 🔍 ДИАГНОСТИКА                                              │
└─────────────────────────────────────────────────────────────┘

# Проверить права на скрипты
ls -la *.sh *.js

# Проверить порты
sudo netstat -tlnp | grep -E '(80|443|54739|6379|9000)'

# Проверить использование диска
df -h

# Проверить использование памяти
free -h

# Проверить процессы
top
htop

# Проверить Nginx конфигурацию
sudo nginx -t

# Проверить SSL сертификаты
sudo certbot certificates

┌─────────────────────────────────────────────────────────────┐
│ 🔒 БЕЗОПАСНОСТЬ                                             │
└─────────────────────────────────────────────────────────────┘

# Установить правильные права
chmod 600 .env .env.*
chmod 755 *.sh
chmod 644 *.js *.md

# Проверить firewall
sudo ufw status

# Проверить что .env не в git
git status

┌─────────────────────────────────────────────────────────────┐
│ 📚 ДОКУМЕНТАЦИЯ                                             │
└─────────────────────────────────────────────────────────────┘

Быстрый старт:          cat START_HERE.md
Шпаргалка:              cat QUICK_UPDATE.md
Полное руководство:     cat UPDATE_GUIDE.md
Первая установка:       cat FIRST_TIME_SETUP.md
Настройка webhook:      cat WEBHOOK_SETUP.md
Чеклисты:               cat CHECKLIST.md
Информация о скриптах:  cat SCRIPTS_INFO.md
Основная документация:  cat README.md

┌─────────────────────────────────────────────────────────────┐
│ 💡 ПОЛЕЗНЫЕ АЛИАСЫ (добавьте в ~/.bashrc)                  │
└─────────────────────────────────────────────────────────────┘

alias cup='cd /var/www/Convert/src && ./update.sh'
alias cup-check='cd /var/www/Convert/src && ./update.sh --dry-run'
alias cup-force='cd /var/www/Convert/src && ./update.sh --force'
alias cup-info='cat /var/www/Convert/src/update-info.txt'
alias cup-logs='tail -f /var/log/convert-update.log'
alias cup-status='sudo systemctl status convert-webhook'

# Применить:
source ~/.bashrc

┌─────────────────────────────────────────────────────────────┐
│ 🎯 ТИПИЧНЫЕ СЦЕНАРИИ                                        │
└─────────────────────────────────────────────────────────────┘

# Ежедневное обновление
./update.sh --dry-run && ./update.sh

# Экстренное обновление
./update.sh --force && sudo systemctl reload nginx

# Проверка → Обновление → Перезапуск
./update.sh --dry-run
./update.sh
sudo systemctl reload nginx

# Полная переустановка
rm -rf node_modules dist
npm install
npm run build
sudo systemctl reload nginx

┌─────────────────────────────────────────────────────────────┐
│ 📞 ПОМОЩЬ                                                   │
└─────────────────────────────────────────────────────────────┘

1. Прочитайте START_HERE.md
2. Проверьте QUICK_UPDATE.md
3. Используйте CHECKLIST.md для диагностики
4. Создайте Issue: https://github.com/aibro-ctrl/Convert/issues

═══════════════════════════════════════════════════════════════
  Версия: 1.0 | Дата: 1 декабря 2024 | Статус: Готово ✅
═══════════════════════════════════════════════════════════════

# Инструкция по исправлению TypeScript ошибок

## Что уже исправлено:
✅ Удален неиспользуемый импорт React из App.tsx
✅ Экспортирован CryptoContextType из contexts/CryptoContext.tsx
✅ Добавлен импорт AuthProvider в App.tsx

## Что нужно сделать на сервере:

### 1. Установить недостающие пакеты
```bash
cd /var/www/Convert/src
chmod +x fix-ts-errors.sh
./fix-ts-errors.sh
```

Это установит все недостающие пакеты Radix UI, которые требуются для UI компонентов.

### 2. Проверить сборку
```bash
npm run build
```

## Основные типы ошибок и их решения:

### Ошибки импорта React
- **Решение**: Удалить `import React` если он не используется (React 17+)
- Примеры: `components/Chat/ChatRoom.tsx:1`, `components/Chat/DirectMessageChat.tsx:1`, и др.

### Ошибки с toast duration  
- **Причина**: Интерфейс toast не поддерживает duration
- **Решение**: Либо удалить duration, либо обновить интерфейс в `components/ui/sonner.tsx`

### Ошибки с API ответами
- **Причина**: Ответы API не типизированы правильно
- **Решение**: Добавить интерфейсы ответов в `utils/api.ts`:
```typescript
interface ApiResponse<T> {
  [key: string]: T;
}
```

### Неиспользуемые переменные
- **Решение**: Либо удалить, либо добавить префикс `_`:
```typescript
const _unused = value;  // Явно показываем что не используется
```

### Ошибки crypto типов
- **Причина**: Uint8Array<ArrayBuffer> vs ArrayBuffer
- **Решение**: Добавить преобразование типов:
```typescript
const buffer = uint8Array.buffer as ArrayBuffer;
```

## Оставшиеся ошибки (некритичные):

Большинство оставшихся ошибок - это:
1. Неиспользуемые импорты (безопасно игнорировать в dev)
2. Неиспользуемые переменные (не влияет на работу)
3. Отсутствующие типы для API ответов (работает с `any`)

Приложение должно собираться и работать даже с этими предупреждениями.

## Если сборка все еще падает:

1. Очистить кэш:
```bash
rm -rf node_modules package-lock.json
npm install
```

2. Проверить версию TypeScript:
```bash
npm ls typescript
```

3. Если нужно, обновить TypeScript:
```bash
npm install --save-dev typescript@latest
```

4. Попробовать сборку с игнорированием ошибок типов (временно):
```bash
npm run build -- --noCheck
```

## Приоритет исправлений:

1. ✅ **КРИТИЧНО**: Установить пакеты (уже есть скрипт)
2. ✅ **КРИТИЧНО**: Экспортировать CryptoContextType (исправлено)
3. 🟡 **ВАЖНО**: Исправить API типы
4. 🟢 **ОПЦИОНАЛЬНО**: Удалить неиспользуемые переменные
5. 🟢 **ОПЦИОНАЛЬНО**: Удалить неиспользуемые импорты

Для разработки можно работать с некритичными ошибками. Главное - чтобы приложение собиралось (`npm run build`) и работало.

# Быстрый старт (5 минут)

## Вариант 1: Интерактивная установка (Рекомендуется) 🚀

```bash
# Запустите интерактивный скрипт установки
cd backend-setup
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- ✅ Проверит PocketBase и Redis
- ✅ Запросит все необходимые настройки
- ✅ Создаст .env файлы
- ✅ Установит зависимости
- ✅ Создаст коллекции в PocketBase
- ✅ Протестирует подключения

**Готово!** Откройте http://localhost:3000 и зарегистрируйтесь.

---

## Вариант 2: Ручная установка

## Предварительные требования

У вас должны быть установлены:
- Node.js 18+
- PocketBase (запущен на http://127.0.0.1:54739)
- Redis (запущен на localhost:6379)

## Шаг 1: Создайте администратора PocketBase

```bash
# Откройте в браузере
http://127.0.0.1:54739/_/

# Создайте администратора и запомните email/пароль
```

## Шаг 2: Настройте backend

```bash
cd backend-setup

# Создать .env
cat > .env <<EOF
VITE_POCKETBASE_URL=http://127.0.0.1:54739
VITE_REDIS_HOST=localhost
VITE_REDIS_PORT=6379
VITE_REDIS_DB=0
EOF

# Установить зависимости
npm install pocketbase ioredis dotenv

# Создать коллекции (введите email и пароль администратора)
node create-collections.js
```

## Шаг 3: Настройте frontend

```bash
cd ..

# Создать .env
cat > .env <<EOF
VITE_BACKEND_TYPE=pocketbase
VITE_POCKETBASE_URL=http://127.0.0.1:54739
VITE_REDIS_HOST=localhost
VITE_REDIS_PORT=6379
VITE_REDIS_DB=0
VITE_E2EE_ENABLED=true
VITE_REALTIME_ENABLED=true
VITE_FILE_UPLOAD_ENABLED=true
VITE_VOICE_VIDEO_ENABLED=true
VITE_GOD_MODE_ENABLED=true
VITE_ACHIEVEMENTS_ENABLED=true
EOF

# Сгенерировать ключи безопасности
echo "JWT_SECRET=$(openssl rand -base64 32 | tr -d '=+/' | cut -c1-32)" >> .env
echo "ENCRYPTION_KEY=$(openssl rand -base64 32 | tr -d '=+/' | cut -c1-32)" >> .env

# Установить зависимости
npm install

# Запустить
npm run dev
```

## Готово! 🚀

Откройте http://localhost:3000 и зарегистрируйтесь.

**Важно:** Первый зарегистрированный пользователь автоматически получит роль администратора с доступом к режиму "Глаз Бога".

## Проверка

```bash
# PocketBase
curl http://127.0.0.1:54739/api/health

# Redis
redis-cli ping

# Тест подключения
cd backend-setup
node test-connection.js
```

## Что дальше?

- [INSTALL.md](INSTALL.md) - Полная установка на сервере
- [DEPLOY.md](DEPLOY.md) - Production деплой с Nginx
- [CHECKLIST.md](CHECKLIST.md) - Чек-лист проверки
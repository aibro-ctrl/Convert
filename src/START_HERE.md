# 🚀 НАЧНИТЕ ОТСЮДА - Быстрая инструкция для вас

## 👋 Привет! Система автоматического обновления готова!

Я создал полную систему автоматического обновления вашего приложения Конверт из GitHub. Вот что нужно сделать прямо сейчас:

---

## ⚡ ШАГ 1: Сделайте скрипты исполняемыми (ОБЯЗАТЕЛЬНО!)

```bash
cd /var/www/Convert/src

chmod +x update.sh
chmod +x init-git.sh
chmod +x webhook-update.sh
chmod +x webhook-server.js
```

---

## ⚡ ШАГ 2: Загрузите файлы в GitHub

Все созданные файлы нужно добавить в ваш GitHub репозиторий:

```bash
cd /var/www/Convert/src

# Инициализируйте git (если еще не сделано)
git init
git remote add origin https://github.com/aibro-ctrl/Convert.git

# Добавьте все новые файлы
git add .
git commit -m "Add automatic update system

- Add update.sh script for automatic updates
- Add init-git.sh for git initialization
- Add webhook support (webhook-server.js, webhook-update.sh)
- Add comprehensive documentation
- Add .gitignore and .env.example
"

# Отправьте в GitHub
git push -u origin main
```

**Если возникнет ошибка о существующих файлах:**
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

## ⚡ ШАГ 3: Первое использование

### Вариант A: Простое обновление (рекомендуется)

```bash
# Проверьте что будет обновлено (без реального обновления)
./update.sh --dry-run

# Если всё ОК - обновите
./update.sh
```

### Вариант B: Настройте автообновление через Cron

```bash
# Откройте crontab
crontab -e

# Добавьте строку (обновление каждый день в 3:00 ночи):
0 3 * * * cd /var/www/Convert/src && ./update.sh --force >> /var/log/convert-update.log 2>&1

# Сохраните и выйдите (Ctrl+O, Enter, Ctrl+X)
```

### Вариант C: Настройте GitHub Webhook (самый продвинутый)

Это позволит обновлять приложение автоматически при каждом push в GitHub!

См. подробную инструкцию в [WEBHOOK_SETUP.md](WEBHOOK_SETUP.md)

**Быстрая настройка:**
```bash
# 1. Установите express
npm install express

# 2. Сгенерируйте секрет
openssl rand -hex 32
# Сохраните этот секрет!

# 3. Настройте systemd сервис
sudo cp webhook-server.service /etc/systemd/system/
sudo nano /etc/systemd/system/webhook-server.service
# Вставьте ваш секрет в строку WEBHOOK_SECRET

# 4. Запустите сервис
sudo systemctl daemon-reload
sudo systemctl enable convert-webhook
sudo systemctl start convert-webhook

# 5. Настройте webhook в GitHub
# Перейдите: https://github.com/aibro-ctrl/Convert/settings/hooks
# Добавьте webhook с вашим URL и секретом
```

---

## 📚 Что было создано?

### 🔄 Скрипты (4 файла):
1. **update.sh** - Главный скрипт обновления
2. **init-git.sh** - Инициализация Git
3. **webhook-update.sh** - Обновление по webhook
4. **webhook-server.js** - Webhook сервер

### 📖 Документация (9 файлов):
1. **README.md** - Основная документация проекта
2. **QUICK_UPDATE.md** - Шпаргалка (читайте каждый день!)
3. **UPDATE_GUIDE.md** - Полное руководство
4. **DEPLOYMENT.md** - Развертывание
5. **WEBHOOK_SETUP.md** - Настройка webhook
6. **FIRST_TIME_SETUP.md** - Для новых серверов
7. **CHECKLIST.md** - Чеклисты проверки
8. **SCRIPTS_INFO.md** - Детали скриптов
9. **FILES_SUMMARY.md** - Сводка файлов

### ⚙️ Конфигурация (3 файла):
1. **.env.example** - Пример настроек
2. **.gitignore** - Исключения для Git
3. **webhook-server.service** - Systemd сервис

---

## 🎯 Что делать дальше?

### Сегодня (обязательно):
1. ✅ Сделайте скрипты исполняемыми (ШАГ 1)
2. ✅ Загрузите файлы в GitHub (ШАГ 2)
3. ✅ Протестируйте обновление: `./update.sh --dry-run`

### На этой неделе:
4. ✅ Настройте автообновление (Cron или Webhook)
5. ✅ Прочитайте QUICK_UPDATE.md
6. ✅ Создайте `.env` из `.env.example` если еще не сделано

### В будущем:
7. ✅ Изучите остальную документацию
8. ✅ Настройте мониторинг
9. ✅ Регулярно проверяйте логи

---

## 🔥 Быстрые команды для ежедневного использования

```bash
# Проверить изменения без обновления
./update.sh --dry-run

# Обновить приложение
./update.sh

# Обновить без вопросов
./update.sh --force

# Посмотреть информацию о последнем обновлении
cat update-info.txt

# Посмотреть логи обновления (если используете cron)
tail -f /var/log/convert-update.log

# Проверить статус webhook сервера (если настроен)
sudo systemctl status convert-webhook
```

---

## 📖 Какую документацию читать?

### Если вы только начинаете:
1. **START_HERE.md** ← ВЫ ЗДЕСЬ
2. **QUICK_UPDATE.md** - шпаргалка на каждый день

### Если устанавливаете на новом сервере:
1. **FIRST_TIME_SETUP.md** - пошаговая инструкция
2. **CHECKLIST.md** - проверьте все пункты

### Если хотите автоматизацию:
1. **WEBHOOK_SETUP.md** - детальная настройка
2. **UPDATE_GUIDE.md** - все про обновления

### Если возникла проблема:
1. **UPDATE_GUIDE.md** → раздел "Устранение проблем"
2. Проверьте логи
3. Используйте чеклисты

---

## 🎁 Бонусы

### Файлы автоматически НЕ обновляются (безопасно):
- ✅ `.env` - ваши настройки
- ✅ `backend-setup/` - настройки бэкенда
- ✅ `backend-adapter/` - адаптер PocketBase
- ✅ `pocketbase/`, `pb_data/` - база данных
- ✅ Бэкапы создаются автоматически перед каждым обновлением!

### Создается автоматически:
- ✅ `update-info.txt` - информация о последнем обновлении
- ✅ `backups/update-YYYYMMDD-HHMMSS/` - бэкап важных файлов

---

## 🆘 Если что-то пошло не так

### Скрипт не запускается:
```bash
chmod +x update.sh
```

### Git ошибки:
```bash
./init-git.sh
```

### Нужно откатить обновление:
```bash
git log --oneline -10
git reset --hard <commit-hash>
npm install
npm run build
```

---

## 💡 Полезные советы

1. **Всегда используйте `--dry-run` перед обновлением в продакшене**
   ```bash
   ./update.sh --dry-run
   ```

2. **Проверяйте логи после обновления**
   ```bash
   cat update-info.txt
   ```

3. **Делайте бэкап базы данных перед крупными обновлениями**
   ```bash
   cp -r pocketbase/pb_data pb_data.backup-$(date +%Y%m%d)
   ```

4. **Используйте webhook для автоматизации** - это экономит время!

5. **Читайте QUICK_UPDATE.md** - там вся нужная информация на одной странице

---

## 📊 Схема обновления

```
GitHub (push) 
    ↓
Webhook (опционально)
    ↓
update.sh
    ↓
┌─────────────────────────┐
│ 1. Проверка изменений   │
│ 2. Создание бэкапа      │
│ 3. Клонирование         │
│ 4. Копирование файлов   │
│ 5. Установка зависимостей│
│ 6. Сборка приложения    │
│ 7. Сохранение информации│
└─────────────────────────┘
    ↓
Приложение обновлено! ✅
```

---

## 🎉 Готово!

Теперь вы можете:
- ✅ Автоматически обновлять приложение из GitHub
- ✅ Не переносить файлы вручную
- ✅ Иметь бэкапы перед каждым обновлением
- ✅ Откатывать изменения при необходимости

**Следующий шаг:** Запустите `./update.sh --dry-run` прямо сейчас!

---

## 📞 Нужна помощь?

1. Проверьте [QUICK_UPDATE.md](QUICK_UPDATE.md) - шпаргалка
2. Посмотрите [UPDATE_GUIDE.md](UPDATE_GUIDE.md) - полное руководство
3. Используйте [CHECKLIST.md](CHECKLIST.md) - для диагностики
4. Создайте Issue на GitHub

---

**Удачи с обновлениями! 🚀**

*P.S. Не забудьте загрузить все эти файлы в GitHub (ШАГ 2)!*

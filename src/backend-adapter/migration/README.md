# 🚀 Миграционный пакет чата "Конверт"

## 📦 Что это?

Полный набор инструментов для миграции чата "Конверт" с Figma Make (Supabase) на собственный сервер с **PocketBase + Redis**.

## 🎯 Возможности

- ✅ **Автоматическая установка** - один скрипт запускает всё
- ✅ **Экспорт/импорт данных** - сохраняет всю историю
- ✅ **Docker Compose** - легкое развертывание
- ✅ **Резервное копирование** - автоматические бэкапы
- ✅ **E2EE поддержка** - шифрование работает после миграции
- ✅ **Zero downtime** - можно мигрировать постепенно

## 📁 Структура

```
migration/
├── docker-compose.yml      # Конфигурация Docker
├── .env.example           # Шаблон конфигурации
├── install.sh             # Скрипт автоустановки
├── migrate.ts             # Скрипт миграции данных
├── backup.sh              # Скрипт резервного копирования
├── package.json           # NPM зависимости
└── README.md             # Этот файл
```

## ⚡ Быстрый старт

### 1. Автоматическая установка

```bash
chmod +x install.sh
./install.sh
```

### 2. Ручная установка

```bash
# 1. Настройка
cp .env.example .env
nano .env  # Отредактируйте конфигурацию

# 2. Запуск сервисов
docker-compose up -d

# 3. Миграция данных
npm install
npm run migrate:auto

# 4. Проверка
curl http://localhost:8090/api/health
```

## 🔧 Команды

### Миграция данных

```bash
# Полная автоматическая миграция
npm run migrate:auto

# Экспорт из Supabase
npm run migrate:export

# Импорт в PocketBase
npm run migrate:import
```

### Docker управление

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Логи
docker-compose logs -f

# Статус
docker-compose ps
```

### Резервное копирование

```bash
# Ручной бэкап
./backup.sh

# Автоматический бэкап (cron)
0 2 * * * /path/to/backup.sh >> /var/log/konvert-backup.log 2>&1
```

## 🗄️ Сервисы

| Сервис | Порт | URL | Назначение |
|--------|------|-----|------------|
| PocketBase | 8090 | http://localhost:8090 | База данных + API |
| PocketBase Admin | 8090 | http://localhost:8090/_/ | Админ панель |
| Redis | 6379 | localhost:6379 | Кэш + Real-time |
| Nginx (опц.) | 80/443 | https://... | Reverse proxy |

## 📊 Что мигрируется?

### ✅ Данные
- Пользователи (+ пароли, роли, статусы)
- Комнаты (публичные, приватные, DM)
- Сообщения (+ реакции, упоминания, реплаи)
- Достижения
- Запросы дружбы
- E2EE ключи (публичные)

### ⚠️ Требует ручной настройки
- Аватары (требуется скачать и загрузить)
- Файлы в сообщениях
- Голосовые/видео сообщения

### ❌ Не мигрируется
- Активные сессии (нужен повторный вход)
- Приватные ключи E2EE (генерируются при входе)
- Временные данные

## 🔐 Безопасность

### Генерация паролей

Скрипт `install.sh` автоматически генерирует:
- JWT Secret
- Encryption Key
- Redis Password
- PocketBase Admin Password

**Сохраните эти пароли!**

### Рекомендации для продакшена

1. **Измените пароли**
```bash
nano .env
# Обновите все *_PASSWORD переменные
```

2. **Настройте HTTPS**
```bash
# См. MIGRATION_GUIDE.md раздел "Nginx конфигурация"
```

3. **Ограничьте доступ**
```bash
# Firewall
sudo ufw allow 443/tcp
sudo ufw deny 8090/tcp  # Закрыть прямой доступ к PocketBase
```

4. **Резервное копирование**
```bash
# Настройте автоматический бэкап
crontab -e
# Добавьте: 0 2 * * * /path/to/backup.sh
```

## 🐛 Troubleshooting

### PocketBase не запускается

```bash
# Проверка логов
docker logs konvert_pocketbase

# Проверка порта
sudo lsof -i :8090

# Очистка и перезапуск
docker-compose down
rm -rf pb_data
docker-compose up -d
```

### Redis ошибка подключения

```bash
# Проверка
docker exec konvert_redis redis-cli ping

# С паролем
docker exec konvert_redis redis-cli -a PASSWORD ping

# Перезапуск
docker-compose restart redis
```

### Миграция не работает

```bash
# Проверка Supabase ключей
cat .env | grep SUPABASE

# Тест подключения
curl -H "apikey: YOUR_KEY" "YOUR_SUPABASE_URL/rest/v1/"

# Принудительный экспорт
npm run migrate:export -- --force
```

### Данные не импортируются

```bash
# Проверка файла экспорта
ls -lh data/export.json
cat data/export.json | jq '.'

# Проверка аутентификации PocketBase
curl -X POST http://localhost:8090/api/admins/auth-with-password \
  -H "Content-Type: application/json" \
  -d '{"identity":"admin@konvert.chat","password":"YOUR_PASSWORD"}'
```

## 📈 Мониторинг

### Базовый мониторинг

```bash
# Docker статистика
docker stats konvert_pocketbase konvert_redis

# Использование диска
du -sh pb_data/

# Логи в реальном времени
docker-compose logs -f --tail=100
```

### Продвинутый мониторинг

Установите Netdata:
```bash
bash <(curl -Ss https://my-netdata.io/kickstart.sh)
```

Или настройте Prometheus + Grafana (см. документацию)

## 🔄 Обновление

### PocketBase

```bash
# Остановка сервиса
docker-compose down

# Обновление образа
docker-compose pull pocketbase

# Запуск
docker-compose up -d
```

### Redis

```bash
# То же самое
docker-compose pull redis
docker-compose up -d
```

## 📚 Документация

- [MIGRATION_GUIDE.md](../../MIGRATION_GUIDE.md) - Полное руководство
- [QUICK_START.md](../../QUICK_START.md) - Быстрый старт
- [PocketBase Docs](https://pocketbase.io/docs/)
- [Redis Docs](https://redis.io/docs/)

## 💡 Советы

### Производительность

1. **Увеличьте память Redis**
```yaml
# docker-compose.yml
redis:
  command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
```

2. **Оптимизируйте PocketBase**
- Добавьте индексы на часто используемые поля
- Настройте кэширование в Admin UI

3. **Используйте SSD**
- PocketBase работает в 2-3 раза быстрее на SSD

### Масштабирование

Для больших нагрузок:
- Вынесите Redis на отдельный сервер
- Используйте Redis Cluster
- Настройте load balancer (Nginx/HAProxy)
- Рассмотрите репликацию PocketBase

## ❓ FAQ

**Q: Можно ли вернуться на Supabase?**  
A: Да, просто измените `VITE_BACKEND_TYPE=supabase` в `.env`

**Q: Потеряются ли данные?**  
A: Нет, миграция сохраняет все данные. Но сделайте бэкап!

**Q: Нужно ли пересоздавать пароли?**  
A: Нет, хеши паролей мигрируются. Но рекомендуется сброс для безопасности.

**Q: Работает ли E2EE?**  
A: Да, но пользователи должны войти заново для генерации приватных ключей.

**Q: Сколько времени занимает миграция?**  
A: ~10-30 минут в зависимости от объема данных.

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте [Troubleshooting](#troubleshooting)
2. Просмотрите логи: `docker-compose logs`
3. Проверьте конфигурацию: `cat .env`
4. Создайте issue с описанием проблемы

---

**Версия:** 1.0.0  
**Лицензия:** MIT  
**Автор:** Konvert Chat Team

**Успешной миграции!** 🎉

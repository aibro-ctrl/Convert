# 🚀 Первый запуск приложения "Конверт"

## Важно! Прочтите перед использованием

При первом запуске приложения **база данных пуста** и в ней **нет пользователей**. Это означает, что вы не сможете войти в систему, пока не создадите учетную запись.

## Быстрый старт (2 способа)

### Способ 1: Создать тестового пользователя (рекомендуется для тестирования)

1. На экране входа нажмите на иконку **⚙️ (шестеренка)** в правом верхнем углу
2. Откроется **Панель разработчика** (DevPanel)
3. Нажмите кнопку **"✨ Создать тестового пользователя"**
4. Тестовый пользователь будет создан с данными:
   - **Email:** test@example.com
   - **Пароль:** test12345678
5. Закройте панель разработчика (нажмите снова на ⚙️)
6. Войдите используя эти тестовые данные

### Способ 2: Зарегистрировать собственного пользователя

1. На экране входа нажмите **"Зарегистрироваться"**
2. Заполните форму регистрации:
   - **Имя пользователя** (минимум 3 символа, латиница или кириллица)
   - **Email** (формат: user@example.com)
   - **Пароль** (минимум 8 символов)
   - **Подтверждение пароля**
3. Нажмите **"Зарегистрироваться"**
4. Вы автоматически войдете в систему после успешной регистрации

## Частые проблемы и их решения

### ❌ "Invalid login credentials" или "Неверный email или пароль"

**Причина:** Вы пытаетесь войти с данными пользователя, которого не существует в базе.

**Решение:**
1. Убедитесь, что вы **сначала зарегистрировались** (см. выше)
2. Или создайте тестового пользователя через DevPanel
3. Проверьте правильность написания email и пароля

### ❌ "Пользователь не найден"

**Причина:** База данных не содержит пользователя с указанным email.

**Решение:**
1. Зарегистрируйте нового пользователя
2. Или используйте тестового пользователя через DevPanel

### ❌ "Invalid token" или "missing sub claim"

**Причина:** Проблемы с JWT токеном аутентификации.

**Решение:**
1. Очистите localStorage в браузере
2. Перезагрузите страницу
3. Войдите заново

## Панель разработчика (DevPanel)

DevPanel - это инструмент для управления пользователями и отладки:

### Возможности:
- ✨ **Быстрое создание тестового пользователя** одной кнопкой
- 👥 **Просмотр всех пользователей** в системе
- 🗑️ **Удаление пользователей** (для очистки тестовых данных)
- ✅ **Проверка синхронизации** между Supabase Auth и KV хранилищем

### Как открыть DevPanel:
1. На экране входа найдите иконку **⚙️** в правом верхнем углу
2. Нажмите на неё

### Легенда индикаторов в списке пользователей:
- 🟢 **Зелёная галочка** - пользователь есть в Supabase Auth
- 🔵 **Синяя галочка** - пользователь есть в KV хранилище
- Оба индикатора должны быть активны для корректной работы

## После успешного входа

После входа в систему вы получите доступ к:

### Основной функционал:
- 💬 **Чаты** - личные и групповые переписки
- 🏠 **Комнаты** - публичные и приватные чат-комнаты
- 👤 **Профиль** - управление вашим профилем
- 🔔 **Уведомления** - система уведомлений о новых сообщениях и упоминаниях

### Функции чата:
- **Упоминания:** @username, @admin, @moder, @vip
- **Реакции** на сообщения (эмодзи)
- **Закрепление** важных сообщений
- **Ответы** на сообщения (reply)
- **Опросы** с возможностью анонимного голосования
- **Голосовые** и **видео** сообщения
- **Поиск** по сообщениям

### Роли пользователей:
1. **👤 User** (по умолчанию) - обычный пользователь
2. **⭐ VIP** - привилегированный пользователь с доп. возможностями
3. **🛡️ Moderator** - может банить/мутить пользователей
4. **👑 Admin** - полный доступ, включая режим "Глаз Бога"

## Технические детали

### Архитектура:
- **Frontend:** React + Tailwind CSS
- **Backend:** Supabase Edge Functions (Hono framework)
- **Database:** Supabase KV Store (key-value хранилище)
- **Auth:** Supabase Auth (JWT токены)
- **Real-time:** Polling каждые 3 секунды

### Хранение данных:
Все данные хранятся в единой таблице `kv_store_b0f1e6d5` в формате JSON:
- `user:{userId}` - данные пользователей
- `room:{roomId}` - данные комнат
- `message:{messageId}` - сообщения
- `notification:{userId}:{notificationId}` - уведомления

## Дополнительная помощь

Если у вас возникли проблемы:

1. **Проверьте консоль браузера** (F12 → Console) на наличие ошибок
2. **Очистите кэш и localStorage** браузера
3. **Используйте DevPanel** для проверки состояния пользователей
4. **Пересоздайте пользователя** если что-то пошло не так

## Полезные команды консоли браузера (для отладки)

```javascript
// Очистить localStorage
localStorage.clear();

// Посмотреть текущий токен
console.log(localStorage.getItem('access_token'));

// Посмотреть данные текущего пользователя
console.log(JSON.parse(localStorage.getItem('user') || '{}'));
```

---

**Приятного использования! 🎉**

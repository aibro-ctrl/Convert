# Руководство по исправлению ошибок токена

## Проблема
Ошибки:
- "Invalid token - user not found"
- "getUserFromToken: Supabase validation error: invalid claim: missing sub claim"
- "Token is expired" / "token has invalid claims: token is expired"
- "invalid JWT: unable to parse or verify signature"

## Причины
Эти ошибки возникают когда:
1. **Токен в localStorage поврежден** - не является валидным JWT
2. **Токен истёк** - срок действия истек (обычно через 1 час)
3. **Токен не содержит claim 'sub'** - обязательное поле отсутствует
4. **Данные в localStorage повреждены** - некорректный формат
5. **Токен используется после истечения** - не был автоматически обновлён

## Решение

### Вариант 1: Быстрое решение (Рекомендуется)

1. **Откройте DevPanel** (панель разработчика в приложении)
2. **Нажмите кнопку "🗑️ Очистить localStorage"**
3. **Подтвердите действие**
4. **Приложение перезагрузится**
5. **Войдите заново**

### Вариант 2: Ручная очистка

1. Откройте консоль браузера (F12)
2. Перейдите на вкладку "Application" (Chrome) или "Storage" (Firefox)
3. Найдите "Local Storage"
4. Очистите все данные
5. Обновите страницу (F5)
6. Войдите заново

### Вариант 3: Через консоль браузера

1. Откройте консоль (F12)
2. Выполните команду:
   ```javascript
   localStorage.clear();
   location.reload();
   ```

## Что было исправлено

### На фронтенде (`/contexts/AuthContext.tsx`):
1. ✅ **Добавлена проверка формата JWT** - токен валидируется перед использованием
2. ✅ **Проверка наличия claim 'sub'** - обязательное поле JWT
3. ✅ **Проверка срока действия токена** - автоматическое удаление устаревших токенов
4. ✅ **Полная очистка при выходе** - `localStorage.clear()` при signout
5. ✅ **Автоматическая очистка поврежденных токенов** - при любой ошибке валидации
6. ✅ **Подробное логирование истечения** - показывает когда истёк и когда истечёт токен

### В API клиенте (`/utils/api.ts`):
1. ✅ **Функция isTokenExpired()** - проверяет истечение токена перед запросом
2. ✅ **Автоматическая очистка истекших токенов** - с 60-секундным буфером
3. ✅ **Обработка 401 ошибок** - автоматический logout при неавторизованных запросах
4. ✅ **Автоматический reload страницы** - перенаправление на экран входа
5. ✅ **Сохранение темы при очистке** - UX не страдает

### На бэкенде (`/supabase/functions/server/auth.tsx`):
1. ✅ **Предварительная валидация JWT** - проверка формата перед отправкой в Supabase
2. ✅ **Проверка структуры токена** - 3 части, разделенные точкой
3. ✅ **Декодирование payload** - проверка наличия 'sub' claim
4. ✅ **Проверка срока действия** - валидация 'exp' claim
5. ✅ **Детальное логирование** - подробные сообщения об ошибках

### В API (`/supabase/functions/server/index.tsx`):
1. ✅ **Расширенное логирование** - детальная информация о каждом запросе
2. ✅ **Проверка заголовков** - валидация Authorization header
3. ✅ **Понятные сообщения об ошибках** - "пожалуйста, войдите снова"

### В DevPanel (`/components/Admin/DevPanel.tsx`):
1. ✅ **Кнопка очистки localStorage** - быстрое решение проблем с токеном
2. ✅ **Подсказки для пользователя** - когда использовать очистку

## Как предотвратить проблему в будущем

1. **Не редактируйте localStorage вручную**
2. **Не копируйте токены между устройствами**
3. **Используйте кнопку "Выйти" для корректного выхода**
4. **При любых странных ошибках - очистите localStorage**

## Техническая информация

### Структура валидного JWT токена:
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1c2VyLWlkIiwiZXhwIjoxNzAwMDAwMDAwfQ.signature
```

### Обязательные claims в payload:
- **sub** - Subject (ID пользователя)
- **exp** - Expiration time (время истечения)
- **iat** - Issued at (время выдачи)

### Процесс валидации токена:

```
1. Фронтенд: Получить токен из localStorage
   ↓
2. Фронтенд: Проверить формат JWT (3 части)
   ↓
3. Фронтенд: Декодировать payload
   ↓
4. Фронтенд: Проверить наличие 'sub' и 'exp'
   ↓
5. Фронтенд: Отправить запрос на сервер с токеном
   ↓
6. Бэкенд: Повторить проверки 2-4
   ↓
7. Бэкенд: Валидировать через Supabase Auth
   ↓
8. Бэкенд: Загрузить данные пользователя из KV
   ↓
9. Бэкенд: Вернуть данные пользователя
```

## Диагностика

### Проверка токена в консоли:
```javascript
// Получить токен
const token = localStorage.getItem('access_token');
console.log('Token:', token);

// Декодировать payload
const payload = JSON.parse(atob(token.split('.')[1]));
console.log('Payload:', payload);

// Проверить наличие обязательных полей
console.log('Has sub:', !!payload.sub);
console.log('Has exp:', !!payload.exp);
console.log('Expired:', payload.exp * 1000 < Date.now());
```

## Дополнительная помощь

Если проблема не решена:
1. Проверьте консоль браузера на наличие дополнительных ошибок
2. Проверьте логи сервера в Supabase Dashboard
3. Убедитесь, что используете последнюю версию приложения
4. Попробуйте создать нового пользователя через DevPanel

## Логи для диагностики

При возникновении ошибки вы увидите в консоли:

### Фронтенд (AuthContext):
```
AuthContext: Token payload: { sub: "...", exp: ..., iat: ... }
AuthContext: Token expired - clearing immediately
```

### Бэкенд (auth.tsx):
```
getUserFromToken: Token payload decoded, sub: user-id-here
getUserFromToken: Supabase user validated: user-id-here
getUserFromToken: User data found: username
```

### API (index.tsx):
```
GET /auth/me - Authorization header: Bearer eyJhbG...
GET /auth/me - Token length: 500 First 20 chars: eyJhbGciOiJIUzI1NiI
GET /auth/me - User validated successfully: user-id
```

## Новые исправления (2025-11-01)

### Автоматическая обработка истечения токенов:
- ✅ Добавлена функция `isTokenExpired()` для проверки перед запросами
- ✅ Истекшие токены очищаются автоматически перед использованием
- ✅ При 401 ошибке происходит автоматический logout и reload
- ✅ Буфер 60 секунд предотвращает edge cases
- ✅ Подробное логирование времени истечения токенов

### Подробности:
См. `/JWT-EXPIRATION-FIX.md` для технических деталей

## Статус исправлений
✅ Все исправления применены и протестированы  
✅ Автоматическая валидация токенов включена  
✅ Детальное логирование активировано  
✅ Кнопка очистки добавлена в DevPanel  
✅ Автоматическая обработка истечения токенов (NEW)  
✅ Проактивная проверка перед запросами (NEW)  

**Последнее обновление**: 2025-11-01

# Финальная инструкция по исправлению ошибок TypeScript

## ✅ Что уже исправлено:

### 1. Создана система типов и API обёрток
- `/utils/apiTypes.ts` - типы ответов API (UserResponse, RoomsResponse, DirectMessage, etc.)
- `/utils/simpleApi.ts` - упрощённые обёртки для PocketBase services с правильными типами
- Удалён `/utils/pocketbase/apiWrappers.ts` (был создан по ошибке)

### 2. Обновлен App.tsx
- Используются новые API обёртки из `/utils/simpleApi`
- Импортирован `DirectMessage` из `/utils/apiTypes`
- Исправлены все вызовы API

### 3. Обновлены интерфейсы в `/utils/api.ts`
- `Message`: добавлено `updated_at`, расширен `type` (+ 'image', 'file')
- `User`: добавлено `mute_until`
- Изменён тип headers на `Record<string, string>`

### 4. Экспортирован CryptoContextType
- В `/contexts/CryptoContext.tsx` интерфейс теперь экспортируется

## 📋 Что нужно сделать СЕЙЧАС на сервере:

```bash
cd /var/www/Convert/src

# 1. Установить недостающие пакеты (если ещё не сделано)
npm install --save-dev @types/node

# 2. Попробовать собрать
npm run build
```

## Оставшиеся ошибки и их решение:

Большинство оставшихся ошибок - это неиспользуемые переменные и импорты в компонентах.
Эти ошибки **не критичны** и приложение должно собираться и работать.
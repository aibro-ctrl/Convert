# Backend Setup для Konvert Chat

Скрипты настройки PocketBase и Redis для чата "Конверт".

## Быстрый старт (Интерактивно) 🚀

```bash
# Запустите интерактивный скрипт
chmod +x setup.sh
./setup.sh
```

Скрипт проведет вас через все шаги:

1. Проверит PocketBase и Redis
2. Запросит настройки подключения
3. Запросит учетные данные администратора
4. Создаст .env файлы
5. Установит зависимости
6. Создаст коллекции
7. Протестирует подключения

**📖 Подробнее:**

- [USAGE.md](USAGE.md) - Детальное руководство по использованию скрипта
- [DEMO.md](DEMO.md) - Демонстрация работы скрипта
- [VALIDATION-EXAMPLE.md](VALIDATION-EXAMPLE.md) - Примеры работы валидации учетных данных
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

---

## Ручная установка

```bash
# 1. Убедитесь что PocketBase запущен
curl http://127.0.0.1:54739/api/health

# 2. Создайте администратора PocketBase
# Откройте http://127.0.0.1:54739/_/ в браузере
# и создайте первого администратора

# 3. Создать .env
cp .env.example .env

# 4. Установить зависимости
npm install

# 5. Создать коллекции PocketBase
# Скрипт попросит email и пароль администратора
node create-collections.js

# 6. Проверить подключение
node test-connection.js
```

## Скрипты

- **setup.sh** - Интерактивный скрипт полной установки
- **create-collections.js** - Создание коллекций в PocketBase (использует сохраненный токен)
- **check-admin.js** - Проверка наличия администратора PocketBase
- **validate-admin.js** - Валидация и сохранение токена авторизации (используется в setup.sh)
- **test-connection.js** - Тестирование подключений к PocketBase и Redis

## Процесс авторизации

**Важно:** Авторизация происходит **один раз** при вводе данных!

1. **При вводе** данных администратора `validate-admin.js`:
   - Проверяет учетные данные
   - Сохраняет токен авторизации в `.pb-auth-token`
2. **При создании коллекций** `create-collections.js`:
   - Использует сохраненный токен
   - **Не запрашивает пароль повторно**

Подробнее: [AUTH-FLOW.md](AUTH-FLOW.md)

## Конфигурация

Файл `.env`:

```env
VITE_POCKETBASE_URL=http://127.0.0.1:54739
VITE_REDIS_HOST=localhost
VITE_REDIS_PORT=6379
VITE_REDIS_DB=0
```

## Требования

- PocketBase запущен на http://127.0.0.1:54739
- Redis запущен на localhost:6379
- Node.js 18+

## Создаваемые коллекции

1. **users** - пользователи с ролями и E2EE ключами
2. **rooms** - комнаты (публичные, приватные, DM)
3. **messages** - сообщения с E2EE и реакциями
4. **achievements** - достижения
5. **user_achievements** - разблокированные достижения
6. **friend_requests** - запросы в друзья
7. **files** - загруженные файлы

## Troubleshooting

### PocketBase не доступен

```bash
# Проверить статус
sudo systemctl status pocketbase

# Запустить вручную
cd /opt/pocketbase
./pocketbase serve --http=127.0.0.1:54739
```

### Redis не работает

```bash
# Проверить статус
sudo systemctl status redis-server

# Тест подключения
redis-cli -h localhost -p 6379 ping
```

### Коллекции не создаются

1. Убедитесь что PocketBase доступен: `curl http://127.0.0.1:54739/api/health`
2. Проверьте `.env` файл
3. Запустите с отладкой: `node create-collections.js`